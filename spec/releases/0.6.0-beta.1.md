# PromptHub 0.6.0-beta.1 Release Record

## Release

- Version: `0.6.0-beta.1`
- Tag: `v0.6.0-beta.1`, annotated tag object
  `d665fc5e307f0dd7eb0413fe654784b87a6c13f8`, peeled to commit
  `6c08b5e84d70ecb41b32030b00e2e04fae96319a`
- Date: Original candidate published 2026-08-13 and withdrawn 2026-08-14;
  verified replacement published 2026-08-19
- Channel: Preview / beta
- Status: Published GitHub Prerelease
- Release: https://github.com/legeling/PromptHub/releases/tag/v0.6.0-beta.1
- Original publication CI: https://github.com/legeling/PromptHub/actions/runs/31712011544
- Final Desktop Release CI: https://github.com/legeling/PromptHub/actions/runs/32265536666
- Final Self-Hosted Web CI: https://github.com/legeling/PromptHub/actions/runs/32265536642
- Stable GHCR recovery CI: https://github.com/legeling/PromptHub/actions/runs/32259627093

The maintainer explicitly chose to reuse `0.6.0-beta.1` instead of allocating
`0.6.0-beta.2`. The old annotated tag was moved with an expected-old-value
lease, the existing draft assets were refreshed in place, and publication was
performed as a separate explicit step only after the current local, platform,
artifact, updater-manifest, public-download, and GHCR isolation gates passed.

## Summary

`0.6.0-beta.1` is the first preview of the 0.6 line. It focuses on a unified
Agent management workbench, broader native Agent asset support, provider/model
configuration, official Codex account switching, and file-first local storage
with stronger migration and recovery behavior.

## User-Facing Highlights

- Unified Agent workspaces for Skills, MCP, Plugins, Rules, providers/models,
  configuration files, quotas, and session history.
- In-place selection dialogs for adding Agent Skills, MCP servers, and Plugins.
- Broader Claude Code, Codex, Antigravity, Kimi Code, OpenCode, Pi, Grok Build,
  Qoder, OpenClaw-family, Cursor, and related Agent target coverage.
- Shared provider/model forms with native connection and model probes, explicit
  error explanations, activation safeguards, and account-aware credentials.
- Encrypted Codex official-account snapshots that switch by replacing the
  active `auth.json` without replacing official model or provider settings.
- Unified quota presentation for rolling, weekly, monthly, and total limits,
  including compact circular window indicators and provider membership tiers.
- File-first canonical local authority, rebuildable SQLite catalog behavior,
  journaled recovery, migration safety, and bounded recovery artifacts.

## Release Surface Decisions

- Root, Desktop, CLI, self-hosted Web, Cloudflare Worker, Mobile package, Expo
  metadata, and CLI runtime report `0.6.0-beta.1`.
- Private shared packages retain their independent `0.1.0` versions.
- Public stable badges, default downloads, and website generated metadata remain
  on published stable `0.5.9` until a later stable release is published.
- README preview links may target this explicit prerelease while stable badges,
  default downloads, and update behavior continue to target `0.5.9`.
- Existing public screenshots are retained because neither `docs/imgs/` nor
  `website/public/imgs/` contains an Agent workspace, native quota menu or Agent
  config-editor counterpart. Isolated Electron E2E screenshots verify the
  current Agent/config surfaces without replacing an unrelated stable image.

## Verification

- Focused Core, CLI, Worker, Desktop, release-workflow, version-alignment, and
  website release-metadata tests passed during candidate preparation.
- On 2026-08-19 with local Node `26.7.0`, `pnpm verify:release:quick` passed
  29/29 checks in 1,230.1 seconds.
- On the same frozen candidate, `pnpm verify:release` passed 42/42 checks in
  796.3 seconds, including builds, performance budgets, Desktop
  unit/integration shards, built-artifact E2E, Web smoke, Worker dry-run and
  Mobile checks.
- The isolated Agent config-files E2E passed. The complete Agent workspace E2E
  passed after stale test-only activation-button calls were aligned with the
  current provider-row switches and the test began waiting for asynchronous
  activation-plan resolution; production activation checks were unchanged.
- Replacement candidate commit `e1c0f5bcfa7de5b64cf171f1c02d45cb818728a7`
  passed manual workflow run `31798130196`: full verification, Linux, Windows
  x64 and arm64, and signed/notarized macOS x64 and arm64 all passed. Windows
  x64 also passed the packaged `0.5.9` upgrade cold-start smoke.
- The replacement run was intentionally dispatched without a release tag. Its
  release job was skipped and it did not publish or modify the draft.
- Final tag-triggered Desktop Release run `32265536666` passed the complete
  verification job and Linux, Windows x64, Windows arm64, macOS x64, macOS
  arm64, and Release jobs at commit
  `6c08b5e84d70ecb41b32030b00e2e04fae96319a`. Windows x64 passed the real
  packaged `0.5.9` upgrade cold-start smoke. Both macOS architectures passed
  Developer ID signing, notarization, stapling, and Gatekeeper verification.
- Release job `96116662419` merged updater manifests, verified asset hashes,
  and replaced 20 assets: Linux AppImage and DEB, Windows x64 and arm64
  installers, macOS x64 and arm64 DMG/ZIP payloads and blockmaps, updater
  manifests, and the CLI tarball.
- Final Self-Hosted Web run `32265536642` passed Web verification and Docker
  publication. The beta GHCR manifest digest is
  `sha256:5e83e5cdc63392cf2367e13587d8a1991fe7efe73b776f3383cb6ef6a6784526`;
  only explicit beta/version/SHA tags were published.
- Independent public checks returned HTTP 200 for representative Windows x64
  and arm64, macOS x64 and arm64, Linux AppImage and DEB, CLI, and updater
  manifest URLs. GitHub reports 20 assets, the release as non-draft and
  prerelease, and `v0.5.9` as the unchanged Latest stable release.

## Publication Contract

- The same-version replacement preserved the old remote baseline, moved the
  annotated tag only with an expected-old-value lease, and refreshed same-name
  assets while the release remained a draft.
- GitHub promotion occurred only after all platform, signing, notarization,
  stapling, Gatekeeper, packaged-startup, manifest, asset, and container-tag
  checks passed.
- The published release is a Prerelease, not Latest. Stable-facing `0.5.9`
  links, website metadata, default downloads, and update behavior remain
  unchanged.
- GHCR `latest` and `0.5.9` both resolve to stable manifest digest
  `sha256:110d9c320236a7bc41fc624b564db58b1f71c13a110ee02b198e0024abc3784e`;
  the explicit beta tags resolve to the separate beta digest recorded above.

## Withdrawal Incident

- A Windows user upgrading from `0.5.9` received
  `EPERM: operation not permitted, fsync` before the application window loaded.
- The affected startup path created a pre-upgrade SQLite safety image and then
  attempted to flush a file descriptor opened read-only. Windows requires write
  access for `FlushFileBuffers`, which Node uses for `fsync`.
- Run `31712011544` built Windows x64 and arm64 installers but did not launch a
  packaged Windows application. Its green status therefore did not prove
  Windows startup compatibility.
- The replacement candidate passed the Windows packaged upgrade cold start and
  the complete build matrix, then was explicitly promoted on 2026-08-19 after
  all replacement artifacts and public boundaries were verified.

## Post-Publication Windows Restart Issue

- Reported on 2026-08-20: an overwrite installation opened successfully once,
  but the next Windows launch failed before the main window with
  `SQLite3Error: Could not open the database` on a canonical catalog
  `.db.stage-<pid>-<uuid>` path.
- The release smoke proved only the first upgraded launch. That launch can
  defer canonical authority until renderer persistence migration completes,
  so it did not exercise the failing second-launch checkpoint path.
- Local remediation is tracked by
  `spec/changes/active/windows-canonical-authority-second-launch/`. The public
  prerelease is not yet claimed fixed; a real Windows x64 two-launch workflow
  and replacement publication are required.
- Because the replacement keeps version `0.6.0-beta.1`, clients that already
  installed the affected build will not treat it as a newer automatic update.
  After publication, those users must manually download and overwrite-install
  the fixed package; fresh downloads receive the replacement assets.
