# PromptHub 0.5.9 Release Record

## Release

- Version: `0.5.9`
- Tag: `v0.5.9`
- Date: 2026-07-13
- Channel: Stable

## Summary

`0.5.9` is a stable release focused on Plugin and MCP management, Agent asset sync, canonical Skill install/update handling, custom Prompt output format sequences, signed macOS delivery and in-app updates, expanded Agent targets, complete CLI resource workflows, network configuration, and safer shared database access.

The final stable tag replaces the previously published `v0.5.9` release head so new downloads include the post-release Skill lifecycle, imported-source continuity, package metadata, Agent platform, Web SQLite lock recovery, YAML fidelity, AI protocol, and module-boundary corrections.

## User-Facing Highlights

- Plugin management is promoted into the stable release with My Plugins, Plugin Store, Agent Plugin, install/detail flows, snapshots, source update review, static safety assessment, and child Skill/MCP import.
- MCP management is expanded with the MCP workspace, official template store, Agent target distribution, health checks, selective `.env` import, CLI MCP commands, and resync design.
- Agent asset sync now covers My Skills, My MCP, My Plugins, and Rules across self-hosted sync and backup/restore flows.
- Skill source updates use SHA-256 package fingerprints and three-way reconciliation to reduce false local-change and false up-to-date reports.
- High-risk Skill source updates now return a reviewable report. Users can approve one exact staged package or persist trust only for an exact repository, branch, and directory source; blocked findings, path traversal, and invalid package structures remain non-overridable.
- Remote Git, Zip, content URL, local directory, and Cloud Skill installs and updates share one complete-package lifecycle for staging, review, fingerprint approval, atomic apply, rollback, and structured outcomes across detail, quick, batch, and Git import flows.
- Skills copied from Claude Code or other scanned Agent/project directories preserve their exact source and installed baseline, can update from that original directory later, and roll back temporary records when package/path persistence fails.
- Installed metadata is parsed from the final staged `SKILL.md`; user-owned tags remain local and catalog sentinel versions cannot overwrite the package's actual version.
- Staged self-hosted Skill packages use a bounded source-address check so an unreachable Gitea host is reported as provenance context instead of freezing the update flow.
- SKILL.md YAML frontmatter uses one semantic parser and normalizer across editing, hashing, and source update flows so valid block, nested, and `allowed-tools` fields are retained.
- Plugin updates now require a review/confirmation step before overwriting local content.
- Windows and Agent platform compatibility now includes more paths and platform metadata for Hermes, Trae Work, WorkBuddy, QClaw, and `%LOCALAPPDATA%`.
- Grok Build is available as a built-in Agent target with its official icon and verified `~/.grok` Skill, Rules, configuration, and MCP discovery paths.
- Kimi Code CLI, Reasonix, Augment/Auggie, and ZCode are available as built-in Agent targets with distinct official marks, verified asset roots, and capability-specific MCP boundaries.
- CLI coverage now includes the primary Prompt, Folder, Skill, Rules, MCP, Plugin, and Workspace resource workflows.
- macOS release packaging requires Developer ID signing, notarization, artifact verification, and signed ZIP metadata for direct-install in-app updates.
- CLI and Desktop coordinate writes to the shared SQLite database, recover ownerless legacy locks only after Desktop wins the single-instance gate, and refresh renderer data after returning to the application.
- Prompt output formats can be composed as ordered Prompt-reference sequences and are preserved by backup and restore.
- Core, desktop, and Web now share AI protocol endpoint/auth derivation, preventing provider endpoint and header behavior from drifting across products.
- No source or test file exceeds the 2,000-line hard limit. Larger legacy files are ratcheted through the checked-in size baseline while future files are limited to 1,500 lines by default.

## Release Surface Decisions

- Package versions and CLI runtime version were bumped from `0.5.9-beta.2` to `0.5.9`.
- `CHANGELOG.md`, website changelog, website generated release metadata, root README, and localized README files are aligned to `0.5.9`.
- Stable direct download links point at GitHub Latest stable asset filenames for `0.5.9`; fixed CDN mirror filenames remain deferred until mirror sync is verified.
- The previous `0.5.9-beta.2` prerelease remains linked as a historical preview row in README download tables.
- Screenshots were not regenerated during this release-prep pass; README screenshot copy now describes the current `0.5.9` stable surfaces.
- The stable tag is intentionally moved from the earlier release-preparation commit to the final `0.5.9` release head so the published artifacts include the late CLI, updater, Agent platform, Skill lifecycle, Web lock recovery, database coordination, and verification fixes.
- The replacement release recreates the published `v0.5.9` record and replaces its assets while preserving the stable release URL for installed clients and external links; the published timestamp is 2026-07-13.
- A replacement with the same version only refreshes new downloads and manual reinstalls. The updater intentionally requires a strictly higher semantic version, so already installed `0.5.9` clients require `0.5.10` or later for an in-app update.

## Replacement Verification

- Website release sync, focused Skill lifecycle tests, shared/core/DB/Desktop
  typechecks, Desktop lint, spec/index checks, file-size gate, and diff checks
  passed before publication.
- The local quick harness completed through Desktop typecheck and was stopped
  during Desktop unit tests by explicit maintainer instruction; the full local
  harness was skipped. The complete tag workflows were therefore the final
  publication gate.
- Final `v0.5.9` points to
  `5c125e79ba46aefbeeb96d2b2e48effc677a1d0c` and was published at
  `2026-07-13T11:53:49Z` as the stable Latest release.
- Desktop Build and Release run
  `https://github.com/legeling/PromptHub/actions/runs/29247235788` passed Linux,
  Windows x64/arm64, and macOS Intel/Apple Silicon builds. Both macOS jobs
  passed architecture, Developer ID signing, notarization, stapling, and
  Gatekeeper verification.
- The release publishes 20 desktop/CLI assets, including signed macOS ZIPs and
  all seven update manifests. The workflow reconciled manifest hashes and sizes
  against the uploaded binaries before publication.
- Self-Hosted Web run
  `https://github.com/legeling/PromptHub/actions/runs/29247235722` passed 56 Web
  test files / 337 tests, client/server builds, Docker Compose validation, image
  build, image pull, and archive export.
- GHCR tags `0.5.9`, `v0.5.9`, `latest`, and `sha-5c125e7` point to image digest
  `sha256:2084026d562c766c875ac0b420eca20fd4bfda7d74188269d18ff76cd50751d3`.
- Homebrew tap commit `044f557` updates both DMG hashes to the replacement
  assets: Apple Silicon
  `4094663ce2f93e259d073d24cfb423b2a624b4a49f15a0e8c9763434b5c02eec`
  and Intel
  `7ce71d00eb2977f819486cd2bb0a95ea9a75216f3b7613e371408b0b091bbd02`.
- The optional R2 mirror was skipped because publisher credentials are not
  configured; GitHub Release, in-app updater metadata, Homebrew, and GHCR are
  complete.
